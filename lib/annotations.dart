/// Annotation to measure the CPU active time in one class for each method
class MeasureCpuActiveTime {
  const MeasureCpuActiveTime();
}

/// Annotation to create a builder containing decorators
class WithBuilder {
  const WithBuilder();
}
